<nav class="navbar navbar-expand-md navbar-dark bg-dark main-nav">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="flex-1">
            <!--spacer-->
        </div>
        <div class="order-first order-md-0 d-flex justify-content-center">
            <a class="navbar-brand" href="{{ url('main') }}">
                <img src="{{ asset('images/logo.png') }}" class="logo"  alt="">
            </a>
        </div>
        <div class="collapse navbar-collapse flex-1">
            <ul class="nav navbar-nav ml-auto">
                <li class="nav-item">
                    <form class="form-inline">

                        {% if app.user %}
                            {% if is_granted('ROLE_ADMIN') %}
                                <a class="btn btn-outline-success my-2 my-sm-0" id="botonAdmin" style="margin-right: 5vh;" href='{{ path("easyadmin") }}'><i class="fas fa-tools"></i></a>
                            {% endif %}

                            <a class="btn btn-outline-danger my-2 my-sm-0" id="botonCerrarSesion"  href='{{ path("app_logout") }}'><i class="fas fa-sign-out-alt"></i></a>
                        {% else %}
                            <a class="btn btn-outline-info my-2 my-sm-0" id="botonIniciarSesion" style="margin-right: 5vh;" href='{{ path("app_login") }}'><i class="fas fa-user"></i></a>
                        {% endif %}



                    </form>
                </li>
            </ul>
        </div>
    </div>
</nav>